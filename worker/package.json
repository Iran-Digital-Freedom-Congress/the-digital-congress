{
  "name": "congress-signup",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute congress_signups --file=./migrations/001_create_signups.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
